cmake_minimum_required(VERSION 3.1)
project(blink CXX)
add_executable(blink main.cpp)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}/pigpio)
if(DEFINED RASPI)
  target_link_libraries(blink ${CMAKE_CURRENT_SOURCE_DIR}/pigpio/libpigpio.so)
else(DEFINED RASPI)
endif(DEFINED RASPI)